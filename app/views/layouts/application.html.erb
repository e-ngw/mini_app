<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title><%= page_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!-- Bootstrap 5 CDN（必要であれば）-->
    <%#<link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />%>
  </head>

  <body class="<%= user_signed_in? ? 'with-sidebar' : '' %>">
    <%= render 'shared/header' %>
    <% if user_signed_in? %>
      <%= render 'shared/navbar' %>
    <% end %>
    <%= render 'shared/flash_message' %>
    <%= yield %>
    <div id="back"  data-controller="scroll">
      <a href="#" data-action="click->scroll#scrollToTop">
        <%= image_tag "scroll-up.png", alt: "トップへ戻るアイコン", data: { turbo: false }, class: "arrow" %>
      </a>
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>